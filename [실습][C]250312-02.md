```c
#include <stdio.h>
void practice27();
int max(int, int);
int min(int, int);

int main() {
  practice27();
	return 0;
}

void practice27() {
	int(*fpmm) (int,  int) = null;
  int num1, num2, flag;
  printf("두 개의 숫자를 입력: ");
  scanf("%d %d", &num1, &num2);
  printf("원하는 값을 입력(1 큰값, 2 작은값) : ");
  scanf("%d", &flag);
  if(flag==1){
    fpmm = max;
    printf("큰 값 : %d", fpmm(num1, num2));
  }else{
    printf("작은 값 : %d", fpmm(num1, num2));
  }
}

int max (int x, int y){
  if(x>y) return x;
  return y;
}

int min (int x, int y){
  if(x<y) return x;
  return y;
}
```
