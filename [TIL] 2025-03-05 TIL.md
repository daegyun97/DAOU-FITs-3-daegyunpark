## TIL : C언어 기본
## 학습날짜 : 2025년 3월 05일

### 오늘 내가 새롭게 배운 것은 무엇인지?
 - C의 주석: //는 한 줄 주석, /* */는 여러 줄 주석
 - 컴파일 과정: C는 컴파일, 링크, 로드 단계를 거치며, Java는 JVM이 운영체제와 상호작용함
 - 메모리 실행 방식: C의 코드는 운영체제가 실행하며, Java는 JVM이 필요할 때 운영체제에 요청함
 - int 범위: -2147483648부터 2147483647까지이며, 최솟값(-2147483648)은 표현 가능하지만 -2147483649는 범위를 벗어남
 - sizeof 연산자: sizeof(int), sizeof(short), sizeof(long) 등으로 데이터 타입 크기 확인 가능
 - 과학적 표기법: 1.4e+3은 1.4 × 10^3 = 1400을 의미함
 - 정수 오버플로우: unsigned int는 양수만 저장하므로 max + 1을 저장해도 음수가 나오지 않음
 - 8진수 표기법: printf("%o", 0261);은 8진수 261을 10진수로 변환하여 출력함
 - 자료형 크기: int(4바이트), float(4바이트), char(1바이트), double(8바이트)
 - 입력 함수: scanf() 사용
 - 입력값 형식 불일치: scanf("%d", &num); 후 printf("%.2f", num);을 하면 형식 불일치로 예상과 다른 결과 발생 가능
 - 문자 입력: scanf_s("%c", &ch, 1); 사용 시 버퍼 크기 지정 필요, getchar();과 차이는 버퍼 크기 지정 여부
 - scanf_s 경고: Visual Studio에서는 scanf_s 사용을 권장하며, _CRT_SECURE_NO_WARNINGS로 경고 비활성화 가능
 - 현재 연도 구하기: time.h의 time()과 localtime()을 이용
 - 최댓값, 최솟값 함수: C 표준 라이브러리에 내장된 min, max 함수는 없으며, 매크로나 inline 함수로 직접 구현 필요
 - 전처리기와 헤더 파일: #include로 표준 헤더는 < >, 사용자 정의 헤더는 "" 사용
 - #pragma once: 헤더 파일의 중복 포함 방지를 위한 전처리 지시문
 - 내장 식별자 표기: _대문자 형식은 내장 함수 또는 변수에 사용됨
