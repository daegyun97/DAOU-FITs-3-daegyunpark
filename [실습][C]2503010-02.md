```c
#define _CRT_SECURE_NO_WARNINGS
#define N 5
#define SIZE 5
#include <stdio.h>
#include <stdbool.h>
#include <math.h>
#include <time.h>
#include <stdlib.h>


int main() {
	int f[N] = { 3, 0, -30, -20, 01 };
	int i, min;
	min = f[0];
	for (int i = 1; i < N; i++) {
		if (min > f[i]) {
			min = f[i];
		}
	}

	printf("어는 점 목록:");
	for (i = 0; i < N; i++) {
		printf("%4d ", f[i]);
	};

	printf("\n최소값 : %d", min);
 }
```
