```c
#include <time.h>
#include <stdio.h>


int main() {
	union year {
		int year;
		int grade;
	};
	typedef struct student {
		char name[20];
		char sex;
		int stid;
		union year y;
	}ST;

	ST st1 = { "kdhong", 'm', 1508001 };
	st1.y.grade = 4;
	printf("%d 학생은 %d 학년 입니다", st1.name, st1.y);
	printf("%d 학생은 %d 학년 입니다", st1.name, st1.y.grade);
	printf("%d 학생은 %d 학년 입니다", st1.name, st1.y.year);
	st1.y.year = 1;
	printf("%d 학생은 %d 학년 입니다", st1.name, st1.y);
	printf("%d 학생은 %d 학년 입니다", st1.name, st1.y.grade);
	printf("%d 학생은 %d 학년 입니다", st1.name, st1.y.year);

	return 0;
}
```
