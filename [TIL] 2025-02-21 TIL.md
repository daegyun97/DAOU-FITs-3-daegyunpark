## TIL : 통합 개발
## 학습날짜 : 2025년 2월 21일

### 오늘 내가 새롭게 배운 것은 무엇인지?
 1. AJAX 요청 및 응답 처리
     - 클라이언트는 $.ajax() 메서드를 사용해 서버에 비동기적으로 데이터를 전송
     - 서버는 클라이언트의 요청을 처리한 후 JSON 형식으로 결과를 반환
     - 이를 통해 페이지 리로드 없이 서버와 클라이언트 간의 데이터 교환을 수행
<br/>

 2. 데이터베이스 CRUD 처리
     - 데이터베이스에서 INSERT, UPDATE, DELETE, SELECT 쿼리를 사용하여 데이터를 생성, 수정, 삭제, 조회하는 작업
<br/>

 3. 폼 데이터 전송
     - 클라이언트는 HTML 폼을 통해 사용자가 입력한 데이터를 서버로 전송
     - GET 방식은 URL에 데이터를 포함시켜 전송하고, POST 방식은 데이터를 HTTP 본문에 포함시켜 전송
     - 폼 데이터는 서버에서 처리 후 적절한 응답을 반환
<br/>

 4. JSP 페이지 처리 및 데이터 전달
     - 서버는 request.setAttribute() 메서드를 사용하여 데이터를 JSP 페이지에 전달
     - JSP 페이지에서는 EL(Expression Language) 또는 <%= %> 구문을 사용하여 전달된 데이터를 동적으로 출력
<br/>

 5. 댓글 및 게시글 관리
     - 게시글과 댓글을 생성, 조회, 수정, 삭제하는 기능
     - 게시글 작성 시 INSERT INTO BOARDS 쿼리를 사용하여 게시글을 저장하고 댓글 작성 시 INSERT INTO COMMENTS 쿼리를 사용하여 댓글을 저장
     - 댓글 삭제는 DELETE FROM COMMENTS WHERE id = ? 쿼리로 수행
<br/>

 6. 검색 기능 구현
     - 사용자가 입력한 검색어를 기반으로 게시글을 필터링하는 기능
     - GET 요청을 통해 검색어를 서버로 전달하고 서버는 해당 검색어를 포함한 게시글을 조회하여 반환
<br/>

 7. 페이지네이션(Pagination)
     - 많은 게시글을 페이지 단위로 나누어 표시
     - 사용자는 페이지 번호를 클릭하여 특정 페이지의 게시글을 조회할 수 있음
     - 서버에서는 LIMIT와 OFFSET을 사용하여 데이터베이스에서 필요한 범위의 게시글만 조회하고 이를 클라이언트로 전달
<br/>

 8. 보안 처리
     - SQL Injection과 같은 보안 취약점을 방지하기 위해 PreparedStatement를 사용하여 SQL 쿼리를 작성
     - 사용자의 입력을 검증하고 XSS(교차 사이트 스크립팅) 공격을 방지하기 위해 출력되는 데이터에서 HTML 태그를 필터링
<br/>

 9. 상태 관리
     - 로그인 상태를 관리하기 위해 세션(Session)을 사용하여 사용자 인증 정보를 유지
     - 세션을 통해 사용자가 로그인한 상태에서 게시글 작성 및 댓글 작성 등의 기능을 사용할 수 있도록 보장
<br/>

 10. 컨트롤러와 서비스 계층 분리
     - 컨트롤러는 클라이언트의 요청을 받아 해당 로직을 수행할 서비스 계층에 위임하고 서비스 계층은 비즈니스 로직을 처리한 후 결과를 컨트롤러에 반환
     - 이는 코드의 유지보수성을 높이고 관심사의 분리를 통해 기능별로 관리가 용이하도록 함
