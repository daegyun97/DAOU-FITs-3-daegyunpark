## TIL : MyBatis
## 학습날짜 : 2025년 2월 13일

### 오늘 내가 새롭게 배운 것은 무엇인지?
 - MyBatis의 특징
   - JDBC의 대부분 기능을 제공하여 복잡한 JDBC 코드를 제거하고 깔끔한 코드 유지 가능
   - SQL 실행 결과를 Map 객체에 자동 매핑
   - SQL을 소스코드가 아닌 XML로 관리 → SQL 변경 시 소스 수정/컴파일 없이 반영 가능
   - DataSource 기능 및 Transaction 처리 기능 제공

 - Connection Pool 개념 및 장점
   - WAS에서 다중 thread 환경에서 Database Connection을 미리 생성하여 재활용하는 방식 
   - Connection을 새로 생성하는 대신, 기존 Connection을 빌려주고 반납받아 재사용
   - 속도 향상
   - 자원 공유로 인한 효율적인 리소스 사용
   - Connection 개수를 제어하여 서버 부하 조절 가능
   - 직접 구현하기보다는 기존 Connection Pool 라이브러리를 활용하는 것이 일반적
   - MyBatis도 내부적으로 Connection Pool을 지원하며 설정을 통해 사용 가능

 - MyBatis에서 ${}와 #{}의 차이
   - ${}
     - parameter가 그대로 출력 → 컬럼의 자료형에 맞게 자동 변환됨
     - SQL Injection(쿼리 주입) 위험이 있음 → 사용자 입력값에는 사용 금지
     - 테이블명이나 컬럼명을 동적으로 지정할 때 주로 사용
   - #{}
     - parameter가 SQL 문자열 형태로 들어감 ('문자열' 자동 추가)
     - SQL Injection을 방지할 수 있어 보안적으로 안전
     - 사용자 입력값을 전달할 때 주로 사용
