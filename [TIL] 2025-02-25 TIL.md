## TIL : poject 실습
## 학습날짜 : 2025년 2월 25일

### 오늘 내가 새롭게 배운 것은 무엇인지?
 1. JAX-RS에서 POST 요청 파라미터 처리
     - JAX-RS에서 @POST 요청은 HTTP 본문에서 파라미터를 받음
     - @Consumes(MediaType.APPLICATION_JSON)을 사용하여 요청 본문을 JSON 형식으로 받음을 명시
     -  요청 파라미터를 Java 객체로 변환하기 위해서는 해당 객체에 기본 생성자와 setter 메서드가 필요
<br/>

 2. Setter 메서드 필수 여부
     - JAX-RS에서 요청 본문을 Java 객체로 변환할 때 Jackson 라이브러리는 기본 생성자와 setter 메서드를 사용하여 필드 값을 설정
     - 이와 별도로 @JsonCreator와 @JsonProperty 어노테이션을 사용하면 setter 없이 생성자로 값 할당이 가능

<br/>

 3. 415 Unsupported Media Type 에러
     - 415 Unsupported Media Type 에러는 클라이언트가 서버가 지원하지 않는 형식으로 데이터를 전송했을 때 발생
     -  예를 들어 JSON 형식으로 전송해야 할 데이터를 다른 형식으로 전송하는 경우 발생

<br/>

 4. POST 요청 Body 데이터 전송 방법
     - POST 요청의 본문에 데이터를 보내는 방법은 여러 가지가 있으며 Postman 등의 도구를 이용하여 Content-Type: application/json 헤더와 함께 JSON 데이터를 전송해야 함
     - 요청 본문에 JSON 형식의 데이터를 포함시킬 수 있음
