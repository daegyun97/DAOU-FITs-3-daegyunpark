<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE configuration
  PUBLIC "-//mybatis.org/DTD Config 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-config.dtd">

<configuration>
  <properties resource="./driver.properties"/>

  <settings>
    <setting name="jdbcTypeForNull" value="NULL"/>
  </settings>

  <typeAliases>
    <typeAlias  alias="Book" type="org.example.servletproject.vo.Book"/>
    <typeAlias  alias="SearchedBook" type="org.example.servletproject.dto.BookSearchDTO"/>
  </typeAliases>

  <environments default="development">
    <environment id="development">
      <transactionManager type="JDBC"/> <!-- 수동 제어, MANAGED 자동 -->
      <dataSource type="POOLED">
        <property name="driver" value="${db.driver}"/>
        <property name="url" value="${db.url}"/>
        <property name="username" value="${db.username}"/>
        <property name="password" value="${db.pwd}"/>
      </dataSource>
    </environment>
  </environments>

  <mappers>
    <mapper resource="./sqlmap/Book.xml"/>
  </mappers>
</configuration>