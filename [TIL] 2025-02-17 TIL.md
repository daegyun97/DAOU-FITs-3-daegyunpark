## TIL : Servlet
## 학습날짜 : 2025년 2월 17일

### 오늘 내가 새롭게 배운 것은 무엇인지?
 1. 서블릿 기본 구조
     - HttpServlet 클래스 상속
     -  doGet, doPost 메소드 오버라이딩
     -  서블릿 매핑을 위한 @WebServlet 어노테이션 사용
       <br/>

 2. HTTP 요청 처리
     - req.getParameter() 메소드로 요청 파라미터 추출
     - req.setCharacterEncoding("utf-8")로 한글 인코딩 처리
     - GET 요청 시 클라이언트에서 전달한 데이터를 추출하여 변수에 저장
    <br/>

 3. 서비스 로직 호출
     - 서비스 계층에서 비즈니스 로직을 처리
     - 요청 파라미터를 기반으로 필요한 데이터를 조회하기 위해 DTO(Data Transfer Object) 사용
    <br/>

 4. 응답 생성
     - HttpServletResponse 객체를 통해 응답의 콘텐츠 타입을 설정
     - resp.getWriter()로 PrintWriter 객체 생성
     - 동적 HTML을 생성하여 클라이언트에 전달
    <br/>

 5. HTML 출력
     - for 반복문을 사용하여 서버에서 가져온 데이터를 HTML 테이블에 동적으로 출력
     - 리스트 항목을 반복하여 테이블의 각 행(<tr>)에 표시
     <br/>

 6. 클릭 이벤트 처리 및 페이지 이동
     - 사용자가 테이블의 행을 클릭하면 JavaScript를 이용해 새 페이지로 이동하도록 설정
     - 클릭된 항목에 대한 구체적인 정보를 URL 파라미터로 전달하여 해당 항목의 상세 정보를 보여주는 페이지로 이동
    <br/>

 7. 서블릿 생명주기 메소드
     - 서블릿의 초기화 작업을 init() 메소드에서 수행
     - 서블릿 종료 시 자원 해제 작업을 destroy() 메소드에서 수행
     - 서블릿의 생성과 종료 시 자동으로 호출
    <br/>

 8. 자원 해제 및 종료 작업:
     - destroy() 메소드에서 서블릿 종료 시 데이터베이스 연결이나 열린 리소스를 닫는 등의 자원 해제 작업 수행
     - 서블릿이 종료되기 전에 호출
       <br/>
