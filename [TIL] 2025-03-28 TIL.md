## TIL : C 프로젝트
## 학습날짜 : 2025년 3월 28일

### 오늘 내가 새롭게 배운 것은 무엇인지?
1. 동적 배열 반환 문제
   - 함수 내부에서 offers 배열을 출력하면 정상적으로 보이지만 반환 후에는 데이터를 읽을 수 없음.
   - 원인: 메모리 주소가 달라지거나, 반환된 데이터가 올바르게 유지되지 않음.
<br/>

2. 포인터 전달 및 접근 문제
   - check_trade(&offers[i])를 호출했을 때, 내부에서 offer->id 등의 정수형 필드는 접근되지만, 문자열 필드는 접근되지 않음.
   - 원인: offer 구조체의 문자열 필드가 올바르게 초기화되지 않았거나, 메모리 할당 문제가 발생했을 가능성이 있음
<br/>

3.OCIStmtFetch2 탈출 조건 수정 요청
   - status = OCIStmtFetch2(...) == OCI_SUCCESS || status == OCI_SUCCESS_WITH_INFO 형태로 반복문 탈출 조건을 구성하는 코드 요청.

4. ORA-01460 오류 발생 문제
   - OCIBindByPos에서 offer->id를 바인딩하는 과정에서 "요구된 변환은 실행될 수 없습니다" 오류 발생.
   - 원인: SQLT_INT를 사용했음에도 바인딩이 제대로 되지 않음 → sizeof(offer->id) + 1 대신 sizeof(int)로 수정 필요.
<br/>

5. 출력 포맷 정렬 요청
   - printf로 테이블 형식의 출력 시 칸 수를 맞춰서 예쁘게 정렬하는 코드 요청.
   - %-20s, %-10s 등의 서식 지정자를 활용하여 출력 정렬을 맞춤.
