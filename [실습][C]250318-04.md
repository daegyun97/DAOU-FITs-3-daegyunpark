```c
int main() {
    int* p1 = malloc(sizeof(int));
    *p1 = 3;

    int* p2 = realloc(p1, sizeof(int)*4);

    printf("p1: %p, %d\r\n", p1, *p1);
    printf("p2: %p, %d", p2, *p2);

    //free(p1); 런타임에러 realloc에서 자동으로 free
    free(p2);
    
}
```
