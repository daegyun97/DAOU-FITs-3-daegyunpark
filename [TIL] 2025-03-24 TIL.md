## TIL : C연산자/제어문
## 학습날짜 : 2025년 3월 6일

### 오늘 내가 새롭게 배운 것은 무엇인지?
헤더 파일의 #ifndef와 #pragma once 차이점

#ifndef는 헤더 파일의 중복 포함을 방지하기 위한 매크로 정의 방법이고, #pragma once는 파일이 한 번만 포함되도록 지시하는 방법입니다.

둘 다 중복 포함을 막기 위한 용도로 사용되지만, #pragma once는 특정 컴파일러에서만 지원하고, #ifndef는 표준에 맞는 방법입니다. 그래서 둘 다 사용하기도 합니다.

C에서 static의 사용

static 키워드를 사용하면 함수 내에서만 접근 가능한 변수로 제한됩니다. 다른 함수에서는 접근할 수 없습니다.

전역 변수나 함수에 static을 붙이면 해당 파일에서만 접근 가능하고 다른 파일에서는 사용할 수 없습니다.

LNK2005 오류 문제

LNK2005는 같은 이름의 변수가 여러 번 정의된 경우 발생하는 링크 오류입니다.

extern을 사용하여 외부 변수 선언을 명시해 주거나, 변수 정의를 헤더 파일이 아닌 소스 파일에서만 해야 합니다.

Oracle CREATE TABLE에서 자동 증가하는 ID 필드 설정

ID 필드를 자동 증가하도록 설정하는 방법을 문의하셨습니다. AUTO_INCREMENT 또는 **SEQUENCE**와 **TRIGGER**를 사용할 수 있다는 설명을 드렸습니다.

.dat 파일이 이진 파일인지 여부

.dat 파일은 일반적인 데이터 파일 확장자로, 텍스트 파일일 수도 있고 이진 파일일 수도 있습니다. 실제 내용에 따라 달라집니다.

C 코드에서 Stock 구조체 배열을 CSV 파일로 저장하는 코드 작성

Stock 배열의 데이터를 CSV 파일로 저장하는 방법을 예시 코드와 함께 제공하였습니다.
