## TIL : C 동적 메모리 할당
## 학습날짜 : 2025년 3월 18일

### 오늘 내가 새롭게 배운 것은 무엇인지?
1. 배열과 포인터
    - 배열은 메모리에서 연속된 공간을 차지
    - 포인터는 메모리 주소
    - 배열의 이름은 배열의 첫 번째 요소에 대한 포인터로 취급
    - 배열 선언과 포인터 사용 차이: 배열은 메모리에서 고정된 크기를 가지며 포인터는 동적으로 크기를 조정
    - 메모리 할당: malloc, calloc, realloc 등을 통해 동적으로 메모리를 할당할 수 있으며 배열도 포인터처럼 처리
<br/>

2. 배열에 대한 메모리 할당
   - ST* arr = (ST*)malloc(n * sizeof(ST));와 같은 방식으로 구조체 배열을 동적으로 할당할 때, 포인터 arr는 구조체 배열의 첫 번째 요소를 가리키는 포인터
   - arr[i]는 각 구조체 객체를 나타내며 배열처럼 동작하는 포인터
<br/>

3. 구조체 대입과 얕은 복사 문제
   - 구조체 대입(arr[i] = cus;) 시 얕은 복사 문제가 발생
   - 포인터를 포함하는 구조체를 대입할 경우 두 구조체가 같은 메모리 주소를 가리키게 되며 하나를 수정하면 다른 구조체도 영향
   - 이 문제를 해결하려면 동적 메모리 할당 후 각 구조체의 포인터를 따로 할당하여 깊은 복사 방식으로 처리
<br/>

4. scanf와 구조체의 멤버들
   - 구조체의 멤버를 scanf를 사용하여 입력받을 때 문자열의 경우 자동으로 주소를 참조하므로 주소를 명시하지 않아도 됨
   - 하지만 정수형 변수는 명시적으로 &를 붙여야 함
   - scanf("%s", st.name);는 문자열 name의 시작 주소를 참조하지만 scanf("%d", &st.score);는 score의 메모리 주소를 전달해야 함
<br/>

5. 이차원 배열과 포인터
   - 이차원 배열을 동적으로 할당할 때 int** arr 방식은 포인터 배열을 사용해 각 행에 대해 메모리를 할당
   - 1차원 배열처럼 처리하려면 연속적인 메모리 공간을 할당하고 인덱스 변환을 통해 2D 배열처럼 사용
<br/>

6. calloc과 malloc
   - malloc은 할당된 메모리 공간을 초기화하지 않지만 calloc은 메모리 공간을 0으로 초기화
<br/>

