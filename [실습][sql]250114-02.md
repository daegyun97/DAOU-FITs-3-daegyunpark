```
CREATE TABLE ACCOUNT_INFO (
	ACCOUNT_ID	NUMBER PRIMARY KEY,
	CUSTOMER_Id	NUMBER,
	ACOUNT_NUMBER	VARCHAR2(50),
	BALANCE	NUMBER,
	OPENING_DATE	TIMESTAMP,
	ACCOUNT_STATUS	NUMBER,
	CLOSING_DATE	TIMESTAMP
);
INSERT INTO ACCOUNT_INFO
VALUES (1, 1, '1111-11-1111', 1, SYSTIMESTAMP,1,SYSTIMESTAMP);

CREATE TABLE STOCK_DIVIDENDS (
	STOCK_DIVIDEND_ID	NUMBER PRIMARY KEY,
	DIVIDEND_DATE	DATE,
	DIVIDEND_AMOUNT	NUMBER,
	STOCK_ID	NUMBER,
	ACCOUNT_ID	NUMBER
);
INSERT INTO STOCK_DIVIDENDS
VALUES (1,SYSDATE,1,1,1);

CREATE TABLE TRANSACTION_FEES (
	TRANSACTION_FEE_ID	NUMBER PRIMARY KEY,
	ORDER_ID	NUMBER,
	FEE_AMOUNT NUMBER
);
INSERT INTO TRANSACTION_FEES
VALUES(1,1,1);

CREATE TABLE SECURITIES_ORDERS (
	ORDER_ID	NUMBER PRIMARY KEY,
	ACCOUNT_ID	NUMBER,
	STOCK_ID	NUMBER,
	ORDER_AMOUNT	NUMBER,
	COMPLETED_AMOUNT	NUMBER,
	ORDER_DATE	TIMESTAMP,
	COMPLETED_DATE	TIMESTAMP,
	ORDER_TYPE	NUMBER,
	PRICE	NUMBER	
);
INSERT INTO SECURITIES_ORDERS
VALUES(1,1,1,1,1,SYSDATE,SYSDATE,1,1);

CREATE TABLE STOCK_INFO (
	STOCK_ID	NUMBER PRIMARY KEY,
	STOCK_NAME	VARCHAR2(100),
	LISTING_DATE	DATE,
	EPS	NUMBER,
	BPS	NUMBER,
	PER	NUMBER,
	PBR	NUMBER,
	ROE	NUMBER,
	DIDIVEND_YIELD	NUMBER,
	CEO	VARCHAR2(100),
	CREDIT_RATING	NUMBER
);
INSERT INTO STOCK_INFO
VALUES( 1,'TEST', SYSDATE, 1,1,1,1,1,1,'TEST',1);

CREATE TABLE CUSTOMER_GRADES (
	CUSTOMER_GRADES_ID	NUMBER PRIMARY KEY,
	CUSTOMER_GRADES_NAME	VARCHAR2(50)
);
INSERT INTO CUSTOMER_GRADES
VALUES(1, 'A');

CREATE TABLE PROFIT_REPORTS (
	PROFIT_REPORT_ID NUMBER PRIMARY KEY,
	CUSTOMER_ID NUMBER,
	START_DATE DATE,
	END_DATE DATE,
	START_AMOUNT NUMBER,
	END_AMOUNT NUMBER
);
INSERT INTO PROFIT_REPORTS
VALUES(1,1,SYSDATE,SYSDATE,1,1);

CREATE TABLE SECURITIES_TRADES (
	TRADE_ID NUMBER PRIMARY KEY,
	BUY_ORDER_ID NUMBER,
	SELL_ORDER_ID NUMBER,
	TRADE_AMOUNT NUMBER,
	TRADE_DATE TIMESTAMP,
	TRADE_PRICE NUMBER
);
INSERT INTO SECURITIES_TRADES
VALUES (1,1,1,1,SYSDATE,1);

CREATE TABLE INVESTMENT_PORTFOLIO (
	INVESTMENT_PORTFOLIO_ID NUMBER PRIMARY KEY,
	STOCK_ID NUMBER,
	CUSTOMER_ID NUMBER,
	AMOUNT NUMBER,
	AVERAGE_PRICE NUMBER
);
INSERT INTO INVESTMENT_PORTFOLIO
VALUES(1,1,1,1,1);

CREATE TABLE STOCK_PRICE_INFO (
	STOCK_ID NUMBER PRIMARY KEY,
	CURRENT_PRICE NUMBER,
	PREVIOUS_CLOSING_PRICE NUMBER,
	OPENING_PRICE NUMBER,
	HIGH_PRICE NUMBER,
	LOW_PRICE NUMBER,
	TRADING_VOLUME NUMBER,
	TRADING_VALUE NUMBER
);
INSERT INTO STOCK_PRICE_INFO
VALUES(1,1,1,1,1,1,1,1);

CREATE TABLE CUSTOMER_INFO (
	CUSTOMER_ID NUMBER PRIMARY KEY,
	NAME VARCHAR2(50),
	SURNAME VARCHAR2(50),
	REGISTRATION_DATE DATE,
	CUSTOMER_GRADE_ID NUMBER,
	MARKETING_INFORMATION NUMBER(1),
	PERSONAL_INFORMATION NUMBER(1)
);
INSERT INTO CUSTOMER_INFO
VALUES (1,'TEST','TEST',SYSDATE,1,1,1),
(2,'TEST','TEST',SYSDATE,1,1,1);

COMMIT; 
```
![스크린샷 2025-01-14 145853](https://github.com/user-attachments/assets/e5da4245-a02f-4b89-a647-8c3f56b89edf)

