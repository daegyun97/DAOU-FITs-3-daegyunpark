```c
#include <stdio.h>

typedef struct student {
	char name[20];
	char sex;
	int stid;
	int sub1, sub2, sub3;
	double avg;
} ST;

int input(ST* st);

int main() {

	ST st[3] = { { "kin", 'm', 150000,0,0,0,0 },
		{ "park", 'm', 150000,0,0,0,0 },
		{ "choi", 'm', 150000,0,0,0,0 }
	};
	for (int i = 0; i < 3; i++) {
		input2(&st[i]);
		//input2(st[i]); 불가
	}
	for (int i = 0; i < 3; i++) {
		printf("main() 입력한 값은 %d %d %d\n", st[i].sub1, st[i].sub2, st[i].sub3);

	}

	return 0;
}

int input(ST* st) {
	printf("%s 학생의 3 과목 성적을 입력하세요\n", st->name);
	scanf("%d %d %d", &st->sub1, &st->sub2, &st->sub3);
	printf("input() 입력한 값은 %d %d %d\n", st->sub1, st->sub2, st->sub3);
}

```
