```c
int main() {
    task1();
    task2();
    task3();
    printf("%d",test(100));
}
static int test(float n) {
    int ans;
    if (n < 1) {
        return 0;
    }

	return 1 + test(n/2); 
}
static void task1() {
    int n1;
    int n2;
	scanf_s("%d %d", &n1, &n2);
	printf("%d + %d = %d\n", n1, n2, n1 + n2);
    printf("%d - %d = %d\n", n1, n2, n1 - n2);
    printf("%d * %d = %d\n", n1, n2, n1 * n2);
    printf("%d / %d = %f\n", n1, n2, (float)n1 / n2);
}
static void task2() {
    int n;
    scanf_s("%d", &n);
	for (int i = 1; i <= 9; i++) {
		printf("%d * %d = %2d\n", n, i, n * i);
	}
}
static void task3() {
    char str[100];
    char ans;
    scanf("%s", str);
	for (int i = 0; i < strlen(str); i++) {
        if (str[i] > 93) {
			ans = str[i] - 32;
        }
        else {
            ans = str[i] + 32;
        }
        printf("%c", ans);
	}
    printf("\n");
}
```
