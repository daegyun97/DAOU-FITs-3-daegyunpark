## TIL : JAVA 상속, 추상클래스, 인터페이
## 학습날짜 : 2025년 2월 6일

### 오늘 내가 새롭게 배운 것은 무엇인지?
 - abstract class
    - abstract class의 특징 : abstract class는 instance를 생성할 수 없다
    - abstract class를 상속받은 하위 class에서 abstract method를 overriding 한 후 instance 생성
   <br>
 - interface
   - 동일한 기능에 대해서는 같은 method 명을 사용하도록 강제
   - method명은 지정해 주지만, 내부 구현은 각자에 맞게 구현
   <br>
### 오늘 내가 추가로 학습한 내용은?
```java
        Parent obj2 = new Child(3);
//        System.out.println(obj2.c);다이렉트 접근은 어떠한 경우도 불가
        System.out.println(((Child) obj2).c);//.의 연산자 우선순위


        obj2 = (Child) obj2;//형변환 되는 것이 아님
//        System.out.println(obj2.c); 불가
        Child obj3 = (Child) obj2;// 이게 형변환, 새로운 객체 생성이 아님
        System.out.println(obj3.c);

        Parent obj4 = new Parent();
//        Child obj5 = (Child) obj4;// 상위 클라스로 만들면 형변환 불가
//        System.out.println(obj5.c);
```
