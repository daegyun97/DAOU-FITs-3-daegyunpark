```c
	//실습과제 7
	int age;
	char gender;
	double height;

	printf("성별은? (남자라면 M 여자라면 F)");
	scanf_s("%c", &gender,1);
	printf("나이는?");
	scanf_s("%d", &age);
	printf("키는?");
	scanf_s("%lf", &height); 
	printf(" 성별 : %c\n 나이 : %d세\n 키 : %.1lfcm", gender, age, height);

	//수정 getchar()로 버퍼 비움
	printf("나이는?");
	scanf_s("%d", &age);
	printf("키는?");
	scanf_s("%lf", &height);
	printf("성별은? (남자라면 M 여자라면 F)");
	getchar();
	scanf_s("%c", &gender, 1);
	printf(" 성별 : %c\n 나이 : %d세\n 키 : %.1lfcm", gender, age, height);
```
