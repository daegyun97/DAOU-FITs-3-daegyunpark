## TIL : C 프로젝트
## 학습날짜 : 2025년 3월 27일

### 오늘 내가 새롭게 배운 것은 무엇인지?
 - 메모리 할당 해제 여부: strcpy 사용 후 메모리 해제 시 malloc으로 할당된 메모리는 free로 해제해야 하므로 account를 동적 할당하고 사용 후 free(account) 호출 필요
<br/>

 - 중복 문제 발생 원인: 헤더 파일 여러 번 포함 시 중복 정의 발생=> #pragma once 또는 #ifndef/#define 사용하여 중복 포함 방지
<br/>

 - 코드 링크 에러: insert_tradeadd_offer 함수가 정의되지 않았고 외부 심볼을 참조하는 오류 발생=> 함수 정의가 누락된 경우 헤더 파일에 포함된지 확인 필요
<br/>

 - get_account 함수 사용법: get_account 함수는 동적 메모리 할당 후 사용 후 free로 해제해야 함
<br/>

 - holding 구조체에 SQL 쿼리 결과 저장: OCIStmtFetch2와 OCIDefineByPos 사용하여 쿼리 결과를 holding 구조체에 저장 각 필드에 SELECT 쿼리 결과 바인딩
