```c
struct game {
	char name[7];
	int R1, R2, R3;
};
int main() {

	struct game player;
	double avg;
	printf("선수의 이름은?\n");
	scanf("%s", player.name);
	printf("1,2,3 라운드 점수는?\n");
	scanf("%d %d %d", &player.R1, &player.R2, &player.R3);
	avg = (double)(player.R1, player.R2, player.R3) / 3;
	printf("%s선수의 게임결과 평균 %.1f점\n", player.name, avg);

}

```
