```c
#include <stdio.h>

int main() {

	//실습4
	int score[6] = { 99, 89, 98, 88, 85 };
	int* pscore = score;
	int i, sum = 0;
	if (pscore + 2 == score + 2 && pscore + 2 == &score[2])
		printf("두번째 요소의 주소가 모두 같다\n");
	if (*(pscore + 2) == *(score + 2) && *(pscore + 2) == score[2])
		printf("두번째 요소의 값이 같다\n");
	printf("pscore의 값: %p\n", pscore);
	printf("score의 값: %p\n", score);
	printf("pscore의 주소: %p\n", &pscore);
	printf("score의 주소: %p\n", &score);
	for (int i = 0; i < 5; i++) {
		sum += *pscore++;
	}
	printf("sum: %d\n", sum);

	return 0;
}
```
