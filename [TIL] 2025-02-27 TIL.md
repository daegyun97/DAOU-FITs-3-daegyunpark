## TIL : 실습 프로젝트
## 학습날짜 : 2025년 2월 27일

### 오늘 내가 새롭게 배운 것은 무엇인지?
- 스케줄러:
   - ScheduledExecutorService는 Java에서 주기적인 작업을 처리할 때 사용되는 인터페이스
   - Executors.newScheduledThreadPool(1)을 통해 스레드 풀을 생성, 주어진 시간 간격마다 작업을 실행
   - **ScheduledExecutorService**는 Runnable 또는 Callable을 주기적으로 실행할 수 있도록 해주며 scheduleAtFixedRate 또는 scheduleWithFixedDelay 메서드를 통해 설정
   - **newScheduledThreadPool(1)**은 하나의 스레드만 사용할 경우에 적합, 다중 작업을 처리하려면 풀의 크기 조정 팔

- 상속:
  - 부모 클래스와 자식 클래스의 관계는 객체 지향 설계에서 중요한 개념
  - 쿼리 결과를 자바 객체에 매핑하는 과정에서는 resultMap을 사용
  - 부모 클래스와 자식 클래스 간의 매핑을 resultMap으로 설정할 수 있어 확장된 자식 클래스의 필드까지 적절히 매핑

