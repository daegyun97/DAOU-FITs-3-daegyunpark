## TIL : JDBC API 이해 및 DB 연
## 학습날짜 : 2025년 2월 10일

### 오늘 내가 새롭게 배운 것은 무엇인지?
 - JDBC Driver
    - Java 응용 프로그램이 데이터베이스와 상호 작용할 수 있도록 하는 소프트웨어 구성 요소(class & interface)
 - DBC Driver 활용 과정 
    ![image](https://github.com/user-attachments/assets/f96ea712-6c40-4886-815e-56e674f64a58)
 - DBC Driver 활용 주의 사항
   - STATEMENT 생성 과정
     1. 일반 statement 잘 사용하지 않음
      // Statement stmt = con.createStatement();
      // stmt.execute(sql);
     2. Callable statement => stored procedure 호출
     3. prepared statement =>로 사용
      // PreparedStatement ps = con.prepareStatement(sql);
    - SQL 실행과정
       1. executeUpdate() - select 제외한 구문=> return 값 변경된 row 수
       2. executeQuery() - select 문 => return resultSet
    - 결과 처리: resultSet은 지연 로딩, 한번에 메모리에 올리는게 아님 한줄씩 수행하며 디비 연결이 유지 되어야함

   <br>
### 오늘 내가 추가로 학습한 내용은?
 - transaction 작업의 최소 단위 => connection에 설정
 - acid를 지키기 위해
   - Atomicity (원자성) : 모든 작업이 성공하거나, 전부 롤백되어야 한다
   - Consistency (일관성) : 트랜잭션 전후로 데이터의 무결성이 유지되어야 한다
   - Isolation (고립성) : 동시에 여러 트랜잭션이 실행되더라도 서로 간섭하지 않아야 한다
   - Durability (지속성) : 트랜잭션이 완료되면 데이터는 영구적으로 저장되어야 한다
