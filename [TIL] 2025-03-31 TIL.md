## TIL : C 프로젝트
## 학습날짜 : 2025년 3월 31일

### 오늘 내가 새롭게 배운 것은 무엇인지?
OCIStmtFetch2()의 동작 방식

OCIStmtFetch2()는 OCIStmtExecute()로 실행된 결과 집합에서 지정한 개수만큼 데이터를 가져오는 함수

반환값이 OCI_SUCCESS 또는 OCI_SUCCESS_WITH_INFO이면 정상적인 데이터 fetch

반환값이 OCI_NO_DATA이면 더 이상 가져올 데이터가 없음

OCIStmtFetch2() 호출 후 바운드 변수(OCIDefineByPos()로 지정한 변수)에 데이터가 채워짐

OCIStmtExecute()의 역할과 한계

OCIStmtExecute()는 SQL 문을 실행하는 함수로, SELECT 문인 경우 실제 데이터 fetch는 수행하지 않음

OCIStmtExecute()가 성공해도 결과 집합이 없는 경우 OCIStmtFetch2()에서 OCI_NO_DATA가 반환될 수 있음

OCIDefineByPos()의 역할

OCIDefineByPos()는 SQL 결과 집합의 특정 컬럼을 프로그램 변수에 바인딩하는 역할

바운드 변수는 OCIStmtFetch2()가 실행될 때마다 자동으로 갱신됨

정의된 변수의 크기가 적절하지 않으면 데이터 손실이나 예기치 않은 동작이 발생할 수 있음

NULL 값 처리

C에서는 int 타입 변수에 NULL을 저장할 수 없음 (NULL은 포인터 타입)

OCI_INDICATOR를 사용하여 데이터가 NULL인지 확인 가능

OCIDefineByPos()의 indp 매개변수를 사용하여 NULL 여부를 판단해야 함

C에서 구조체 배열에 값 할당하는 방식

배열 요소에 구조체를 직접 할당하는 것은 가능 (trades[index] = { ... };)

하지만 malloc()으로 동적 할당된 포인터 배열을 사용할 경우, 직접 할당 시 메모리 문제가 발생할 수 있음

구조체 멤버 중 문자열을 포함하는 경우 strcpy()를 사용해야 함

OCI에서 fetch 결과가 없는 경우 처리 방법

OCIStmtFetch2()가 OCI_NO_DATA를 반환하면 더 이상 데이터가 없다는 의미

이를 체크하지 않으면 쓰레기 값이 남아 있거나 예상치 못한 동작이 발생할 수 있음

바운드 변수의 초기화를 확실히 하고, OCI_NO_DATA 반환 시 적절한 처리를 해야 함
