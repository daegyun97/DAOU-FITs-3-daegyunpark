```c
#include <stdio.h>
void practice26();

int main() {
  practice26();
	return 0;
}
void practice26() {
	int sum = 10, score[] = { 99, 80, 91, 78, 85 };
	int* psum, * pscore1, * pscore2;
	psum = &sum;
	pscore1 = score;
	pscore2 = score;
	++pscore2;
	pscore2++;

	printf("sum = %d\n", sum); //10
	printf("*psum = %d\n", *psum); //10
	printf("*psum + 1 = %d\n", *psum + 1); //11
	printf("++*psum = %d\n", ++*psum); //11
	printf("*(psum + 1) = %d\n", *(psum + 1));//trash
	printf("*++psum = %d\n", *++ psum);//trash
	printf("====================================\n");
	printf("*pscore1 = %d\n", *pscore1);//99
	printf("*pscore1 + 1 = %d\n", *pscore1 + 1);//100
	printf("*(pscore1 + 1) = %d\n", *(pscore1 + 1));//80
	printf("*pscore2 - *pscore1 = %d\n", *pscore2 - *pscore1);//-8
	printf("pscore2 - pscore1 = %d\n", pscore2 - pscore1);
	printf("pscore1 = %d\n", pscore1);//address
	printf("pscore2 = %d\n", pscore2);//address
}
```
