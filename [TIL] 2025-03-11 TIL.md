## TIL : C 변수/포인터
## 학습날짜 : 2025년 3월 11일

### 오늘 내가 새롭게 배운 것은 무엇인지?
1. '\0' (널 문자)
   - **'\0'**은 널 종료 문자로, 문자열의 끝을 나타냄
   - ASCII 값: 0 (0x00)
   - 문자열 리터럴 끝에 자동으로 포함됨 (예: "Hello" → {'H', 'e', 'l', 'l', 'o', '\0'}).
   - 문자열에서 '\0'이 없으면 제대로 종료되지 않아서 쓰레기 값을 출력할 수 있음
<br/>

2. NULL과 '\0'의 차이
   - '\0': 문자열 끝을 나타내는 문자 (문자 타입 char)
   - NULL: 포인터가 아무것도 가리키지 않음을 나타내는 값 (void* 타입, 보통 0)
<br/>

3. char* (포인터)와 char[] (배열)의 차이
   - char[] (배열): 문자열 데이터를 스택에 직접 저장하며, 값 변경이 가능.
   - char* (포인터): 문자열 리터럴을 읽기 전용 데이터 영역에 저장하므로 값 변경이 불가능.
<br/>

4. 포인터로 선언한 문자열에서 값 변경 가능 여부
   - 문자열 리터럴(예: "Hello")을 가리키는 포인터는 읽기 전용 영역을 참조하므로 값을 변경하면 **런타임 오류(세그멘테이션 폴트)**가 발생할 수 있음.
```c
char* str = "Hello";
str[0] = 'X';  // ❌ 오류 발생
배열(예: char str[] = "Hello")은 스택에 저장되므로 값을 변경할 수 있음.
```

```c
char str[] = "Hello";
str[0] = 'X';  // ✅ 변경 가능
동적 메모리 할당(예: malloc)을 사용하면 힙(Heap) 메모리에 문자열을 저장할 수 있으므로 값을 변경할 수 있음.
```
     - 배열 크기를 고정하지 않고 동적 메모리 할당을 통해 배열 크기를 동적으로 설정하려면 **malloc**이나 **calloc**을 사용해야 함
     - 동적 메모리 할당을 사용하려면 할당한 메모리를 나중에 **free**를 통해 해제해야 함
```c
char* str = malloc(6);  // 동적 메모리 할당
str[0] = 'H';
```
<br/>

5. scanf/scanf_s
   - scanf
     - scanf는 표준 C 라이브러리에서 제공되는 함수로 입력 버퍼의 크기를 자동으로 확인하지 않습니다
     - 버퍼 오버플로우를 방지하지 않기 때문에 사용자가 예상보다 큰 입력을 하게 되면 메모리 손상이나 프로그램 충돌이 발생
  
   - scanf_s
     - scanf_s는 Microsoft가 제공하는 보안 버전의 함수
     - 버퍼의 크기를 명시적으로 지정해야 하며 이를 통해 버퍼 오버플로우를 방지
     - 버퍼 크기를 파라미터로 제공해야 하므로 scanf_s는 보다 안전
<br/>

6. 정적 변수 (Static Variable)
   - 파일 내에서만 유효하며 다른 파일에서는 직접 접근 불가
   - 함수 내에서 선언된 static 변수는 함수가 호출될 때마다 초기화되지 않고 값을 유지
   - 간접 접근을 위해 정적 변수에 대한 함수를 제공하면 다른 파일에서도 해당 함수를 통해 접근 가능
<br/>

7. 전역 변수 (Global Variable)
   - 프로그램 전체에서 유효하며 여러 파일 간에 접근
   - 다른 파일에서 접근하려면 extern 키워드를 사용하여 변수의 선언



