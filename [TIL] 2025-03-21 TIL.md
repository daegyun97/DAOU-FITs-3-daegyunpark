## TIL : C 데이터베이스 연동
## 학습날짜 : 2025년 3월 21일

### 오늘 내가 새롭게 배운 것은 무엇인지?
1. OCI를 사용한 DB 연결
   - OCIEnvCreate를 사용해 환경 핸들 생성
   - OCIHandleAlloc로 오류, 서버, 세션 핸들을 각각 생성
   - OCIServerAttach를 통해 DB 서버에 연결
   - OCILogon2로 사용자 인증을 통해 DB에 로그인
<br/>

2. 쿼리 실행 및 결과 처리
   - OCIStmtPrepare로 SQL 쿼리 준비
   - OCIStmtExecute로 쿼리 실행
   - 쿼리 실행 후 OCIDefineByPos를 사용해 결과를 변수에 바인딩
   - OCIStmtFetch2로 데이터를 한 행씩 가져오며 while문을 통해 데이터를 반복적으로 처리
<br/>

3. 입력 데이터 처리
   - fgets를 사용해 사용자 입력을 받고 strcspn으로 불필요한 개행 문자를 제거
   - 날짜 입력 시 scanf 대신 fgets와 strptime을 사용해 날짜 형식을 처리할 수 있음
<br/>

4. SQL 쿼리 바인딩
   - OCIBindByPos를 사용해 SQL 쿼리의 자리표시자에 값을 바인딩
   - 바인딩을 통해 입력 데이터를 SQL에 전달하고 실행 후 결과를 반환받음
<br/>

5. 오류 처리
   - OCIError로 오류 발생 시 적절한 메시지를 출력하도록 처리
