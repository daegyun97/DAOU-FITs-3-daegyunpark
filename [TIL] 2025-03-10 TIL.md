## TIL : C함수/배열
## 학습날짜 : 2025년 3월 10일

### 오늘 내가 새롭게 배운 것은 무엇인지?

1. 매크로와 static의 차이
   - 매크로 상수(#define)는 컴파일 전에 코드 치환이 이루어지고 메모리에 저장되지 않음
   - static 변수는 프로그램 실행 중 데이터 영역에 저장됨
<br/>


2. C의 실행 코드 저장 위치
   - 실행 파일의 코드(명령어)는 **코드 영역(.text)**에 저장됨
   - 데이터는 데이터 영역(.data/.bss), 스택(stack), 힙(heap) 등에 저장됨
<br/>


3. 전처리기 단계에서 메모리 사용 여부
   - #define 등의 전처리기는 단순한 텍스트 치환 과정이므로 실행 중 메모리를 직접 사용하지 않음
   - 하지만 컴파일 과정에서 토큰을 처리할 때는 내부적으로 메모리를 사용함
<br/>


4. #include <stdbool.h>, <math.h>의 함수 처리 방식
   - #include된 라이브러리 함수는 매크로 치환이 아닌 컴파일 과정에서 해당 헤더의 선언을 참고하여 링크됨
   - 코드 실행 영역 메모리에 올라간다는 뜻
<br/>


5. time_t와 struct tm의 차이
   - time_t는 단일 변수형 타입(보통 long 또는 long long)
   - struct tm은 여러 개의 시간 관련 변수를 포함하는 구조체
<br/>


6. 전역 변수와 static 변수의 차이
   - 둘 다 데이터 영역(.data/.bss)에 저장되며 수명은 프로그램 종료 시까지
   - 차이점:
     - 전역 변수: 모든 파일에서 접근 가능
     - 파일 범위 static 변수: 선언된 파일 내에서만 접근 가능
     - 함수 내 static 변수: 함수 내부에서만 접근 가능하지만, 값은 유지됨
