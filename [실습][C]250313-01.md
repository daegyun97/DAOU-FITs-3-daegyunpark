```c
//추가문제 1번
void program10();
void print_arr(int** , int, int, int);
int main() {

	program10();
	return 0;
}

void program10() {
	int n, m ; 
	int arr[5][5];
	int* parr[5];
	int idx = 6;
	printf("행과 열의 수를 입력하세요");
	scanf("%d %d", &n, &m);
	printf("배열의 초기값는 자동으로 입력됩니다.");
	for (int i = 0; i < n; i++) {
		parr[i] = arr[i];
		for (int j = 0; j < m; j++) {
			arr[i][j] = i * 10 + j;
		}
	}
	printf("\n");
	print_arr(parr, n, m, idx);
	printf("삭제하려는 행의 인덱스 번호를 입력하세요");
	scanf("%d", &idx);
	printf("\n");
	print_arr(parr, n, m, idx);
}
void print_arr(int** parr, int n, int m, int idx) {
	for (int i = 0; i < n; i++) {
		if (idx == i) continue;
		for (int j = 0; j < m; j++) {
			printf("%d ",parr[i][j]);
		}
		printf("\n");
	}
};
```
