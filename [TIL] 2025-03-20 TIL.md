## TIL : C 파일 입출력
## 학습날짜 : 2025년 3월 20일

### 오늘 내가 새롭게 배운 것은 무엇인지?
1. 텍스트 파일에서 특정 라인의 값 수정 가능 여부
   - 기존 라인의 일부만 덮어쓰는 것은 어렵고 일반적으로 새 파일을 생성 후 수정된 내용을 저장한 후 원본 파일을 교체하는 방식이 사용됨
<br/>

2. 잔액 숫자의 자리 수가 고정된 경우
   - 소수점 이하 자릿수가 동일해야 하며 숫자의 총 자리 수도 일정해야 함(예: %.2lf 형식)
   - 고정 길이 저장 방식에서는 새로운 값이 기존 값을 덮어쓸 수 있음
<br/>

3. fseek() 함수의 사용 가능 여부
   - fgets()로 읽어온 데이터는 메모리에 저장된 문자열이므로 파일 위치 이동(fseek())을 적용할 수 없음
   - fseek()은 파일 포인터를 조정하는 함수로 파일을 이진 모드로 다룰 때 주로 사용됨

<br/>

4. sscanf()에서 특정 필드만 가져올 때의 차이점
   - 개별적으로 "Balance: %lf" 또는 "Name: %s"를 사용하면 해당 문자열이 라인의 처음부터 있어야 정상적으로 읽힘
   - "ID: %d, Name: %[^,], Balance: %lf"처럼 포맷을 정확히 맞춰야 전체 값을 한 번에 추출 가능

<br/>

5. 파일을 열 때 자동으로 생성되는지 여부
   - "w" 또는 "wb" 모드로 열면 파일이 없을 경우 자동 생성됨
   - "r" 또는 "rb" 모드는 파일이 존재하지 않으면 실패
