## TIL : C 포인터
## 학습날짜 : 2025년 3월 12일

### 오늘 내가 새롭게 배운 것은 무엇인지?
1. 포인터와 메모리 영역
   - char city[] = "Hanoi";와 char* city2 = "Hanoi";에서 문자열 "Hanoi"는 데이터 영역에 저장되고 배열 city[]는 스택 영역에 저장
   - 배열 city[]는 문자열 복사본을 스택에 저장하고 city2는 데이터 영역에 있는 문자열 리터럴을 가리킴
<br/>

2. 포인터 배열과 이중 포인터
   - char* arr[3]처럼 포인터 배열을 사용하고 이를 이중 포인터로 다룰 수 있는 이유는 이중 포인터(char** p)가 포인터 배열을 가리킬 수 있기 때문
   - p[i]는 arr[i]와 동일한 동작을 하며 이를 통해 배열의 각 문자열을 접근
   - char** p는 포인터 배열 arr의 시작 주소를 가리키며, p[0], p[1], p[2] 등을 통해 배열의 문자열에 접근
<br/>

3.문자열 처리
   - toupper()는 한 번에 한 문자를 대문자로 변환 (문자열 x)
<br/>

4.C에서 배열 크기 및 메모리 관리
   - sizeof 연산자를 사용하면 배열의 크기를 확인할 수 있지만 포인터 배열에서는 유효하지 않음
   - int size = sizeof(arr) / sizeof(arr[0]); c에서는 이렇게 배열의 크기를 구해줘야 함

