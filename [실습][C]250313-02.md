```c
//추가문제 2번
void program9();
typedef void (*operation2)(int*,int,int);  // 함수 포인터 타입 정의
void my_sum(int*, int, int);
void my_max(int*, int, int);
void my_min(int*, int, int);
void my_square(int*, int, int);

int main() {

	program9();
	return 0;
}

void program9() {
	int n;
	operation2 operations2[4] = { my_sum, my_max, my_min,my_square };
	int array[3][3] = {
		{0, 1, 2},
		{3, 4, 5},
		{6, 7, 8}
	};
	int* parr = array;
	while (true) {
		printf("연산 방법 선택하기\n");
		printf("0.합, 1.최댓값, 2.최솟값, 3.제곱\n");
		scanf("%d", &n);
		if(n==4) break;
		operations2[n](parr, 3, 3);
		
	}
}
void my_sum(int* p, int a, int b) {
	int sum = 0;
	for (int i = 0; i < a; i++) {
		for (int j = 0; j < b; j++) {
			sum += *(p + i * b + j);
		}
	}
	printf("%d\n", sum);
};
void my_max(int* p, int a, int b) {
	int max = *p;
	for (int i = 0; i < a; i++) {
		for (int j = 0; j < b; j++) {
			if (max< *(p + i * b + j)) max = *(p + i * b + j);
		}
	}
	printf("%d\n", max);
};
void my_min(int* p, int a, int b) {
	int min = *p;
	for (int i = 0; i < a; i++) {
		for (int j = 0; j < b; j++) {
			if (min > *(p + i * b + j)) min = *(p + i * b + j);
		}
	}
	printf("%d\n", min);
};
void my_square(int* p, int a, int b) {
	for (int i = 0; i < a; i++) {
		for (int j = 0; j < b; j++) {
			printf("%d ", *(p + i * b + j) * *(p + i * b + j));
		}
		printf("\n");
	}
	printf("\n");
};
```
