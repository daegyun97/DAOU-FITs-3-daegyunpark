## TIL : C 함수/배열
## 학습날짜 : 2025년 3월 7일

### 오늘 내가 새롭게 배운 것은 무엇인지?

1. scanf_s와 배열 크기
    - scanf_s 함수는 입력받을 문자열의 배열 크기를 명시적으로 지정해야 한다는 점이 중요
    - 기본 scanf와 달리, scanf_s는 버퍼 오버플로우를 방지하기 위해 배열의 크기를 두 번째 인자로 전달해야 함
       - 예시: scanf_s("%s", str, sizeof(str));처럼 배열의 크기를 함께 넘겨야 안전하게 문자열을 입력받음

<br/> 

2. 동적 메모리 할당
   - C에서는 배열 크기를 컴파일 타임에 고정해야 하며 동적 배열을 사용하려면 malloc 등을 사용해야 함
   - malloc, calloc, realloc 등을 사용하여 동적으로 메모리를 할당하고 사용 후에는 **free**를 통해 메모리를 해제해야 함.
   - 동적 메모리 할당은 프로그램 실행 중에 메모리 크기를 동적으로 관리할 수 있도록 하여 유연성을 제공함
   - C99 이후에는 **변수 길이 배열(VLA)**이 지원되지만 C99 이전 버전에서는 동적 메모리 할당이 필수

<br/>

3. 데이터 입력
   - getchar는 입력 버퍼에서 한 문자씩 읽는 함수이고 scanf_s는 입력 포맷을 지정하여 여러 종류의 데이터를 처리할 수 있음
   - getchar는 버퍼에서 하나의 문자만 읽기 때문에 여러 문자를 처리하려면 반복문을 사용해야 함
   - scanf_s는 배열 크기를 명시적으로 전달해야 하며 이를 통해 버퍼 오버플로우를 방지하는 보안 기능을 제공함
   - getch()는 버퍼를 사용하지 않고 즉시 입력을 읽는 함수로 콘솔에서 입력된 문자 하나를 바로 받아들이는 방식


<br/>

4. C와 자바의 메모리 관리 차이점
   - C는 운영체제에서 직접 메모리를 할당하고 메모리 관리와 관련된 책임이 프로그래머에게 있음
   - 즉 메모리 할당과 해제를 수동으로 처리해야 하며 이를 통해 메모리 사용을 최적화할 수 있지만 관리가 복잡
   - 자바는 **JVM(Java Virtual Machine)**에서 메모리를 관리하며 **가비지 컬렉션(GC)**을 통해 메모리 할당과 해제를 자동으로 처리함
   - 따라서 프로그래머가 메모리 관리에 신경 쓸 필요가 적고 그만큼 안정성은 높지만 메모리 관리의 세밀한 조정이 어려움
