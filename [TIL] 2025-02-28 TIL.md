## TIL : 실습 프로젝트
## 학습날짜 : 2025년 2월 28

### 오늘 내가 새롭게 배운 것은 무엇인지?
- 스케줄러:
   - ScheduledExecutorService를 사용하여 주기적인 작업 실행
   - scheduleAtFixedRate() 메서드로 일정 간격마다 작업 수행
   - 애플리케이션 종료 시 shutdown()을 호출하여 정리 필요

- Java 코드 관련 개념:
  - switch-case 문에서 break 문이 불필요한 경우(return 사용 시)
  - Duration.between(A, B) 사용 시, A부터 B까지의 시간 차이를 계산
  - Long / Long 연산 시 자동으로 정수 연산(버림 처리)

- 오라클 DB 관련:
  - 특정 테이블의 데이터만 삭제하고 구조(스키마) 유지하는 방법
    - TRUNCATE TABLE 테이블명; (롤백 불가, 빠름)
    - DELETE FROM 테이블명; + COMMIT; (롤백 가능)
