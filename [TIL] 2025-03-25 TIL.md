## TIL : C 프로그래밍 종합
## 학습날짜 : 2025년 3월 25일

### 오늘 내가 새롭게 배운 것은 무엇인지?
1. 스레드 생성
   - 스레드는 독립적인 실행 흐름으로 여러 개의 스레드를 생성하여 동시에 작업을 수행할 수 있음
   - Windows에서는 CreateThread와 같은 시스템 호출을 사용하여 스레드를 생성하고 스레드가 실행할 함수와 필요한 파라미터를 지정함
   - 스레드가 실행되면 지정된 작업을 수행하며 프로그램 효율성을 향상시킴
<br/>

2. 스레드 간 동기화
   - 동기화는 여러 스레드가 동시에 같은 자원에 접근할 때 발생할 수 있는 문제를 방지하는 방법
   - 공유 자원에 여러 스레드가 동시에 접근하려 할 때 하나의 스레드가 작업을 마칠 때까지 다른 스레드는 기다리도록 구현함
   - CRITICAL_SECTION은 공유 자원에 대한 접근을 제어하며 한 스레드가 작업을 마칠 때까지 다른 스레드가 접근하지 못하도록 함
   - EnterCriticalSection으로 락을 획득하고 LeaveCriticalSection으로 락을 해제하여 상호 배제를 구현
<br/>

3. 스레드 종료 및 시간 제어
   - 스레드는 작업을 완료하면 자동으로 종료되며 필요 시 종료될 때까지 기다리도록 할 수 있음
   - Sleep을 사용하여 스레드가 일정 시간 동안 대기하도록 할 수 있음
   - Sleep을 통해 다른 스레드가 실행될 시간을 주거나 특정 시간마다 작업을 수행하도록 조정할 수 있음
<br/>

4. 스레드 우선 순위와 작업 제어
   - 스레드 우선 순위는 운영체제의 스케줄러가 스레드를 실행하는 순서에 영향을 미침
   - 우선 순위가 높은 스레드는 더 많은 CPU 자원을 할당받음
   - Sleep과 같은 시간 제어를 통해 스레드 간 작업 배분을 조정할 수 있음
