## TIL : AJAX
## 학습날짜 : 2025년 2월 20일

### 오늘 내가 새롭게 배운 것은 무엇인지?
1. AJAX 요청 
  - AJAX(Asynchronous JavaScript and XML)는 페이지 새로고침 없이 서버와 비동기적으로 데이터를 주고받는 기술
<br/>
2. 기본 구조

```
$.ajax({
  url: "서버 URL",         // 요청할 URL
  type: "GET" or "POST",   // 요청 방식
  data: { key: value },    // 전송할 데이터
  success: function(response) {   // 성공 시 실행
    // 응답 처리
  },
  error: function(xhr, status, error) {  // 오류 시 실행
    // 오류 처리
  }
});

```
<br/>

3. 주요 옵션
  - url: 요청할 서버 URL
  - type: 요청 방식(GET, POST, PUT, DELETE 등)
  - data: 서버로 보낼 데이터 (객체 형식)
  - dataType: 서버 응답 데이터 형식 (예: json, text, xml)
  - success: 요청 성공 시 실행할 함수
  - error: 요청 실패 시 실행할 함수
<br/>

4. 데이터 전송 형식
  - data: 전송할 데이터를 객체 형식으로 전달 ({ key: value })
<br/>
5. 동기/비동기
  - AJAX는 기본적으로 비동기 방식으로 작동. 동기 요청은 async: false 옵션으로 설정 가능 (추천하지 않음)
<br/>

6. 오류 처리
  - error 옵션을 사용하여 요청 실패 시 에러 처리. xhr 객체를 통해 오류 코드와 메시지를 확인
