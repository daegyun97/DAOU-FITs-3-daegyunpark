## TIL : C연산자/제어문
## 학습날짜 : 2025년 3월 6일

### 오늘 내가 새롭게 배운 것은 무엇인지?
1. 2D 배열의 함수 전달 방식
   - 다차원 배열을 함수에 전달할 때 열 크기를 명시해야 함
  ```c
  void func(int arr[][3], int row);
  ```
   - 배열을 1D 포인터(int*)로 변환해서 전달 가능
```c
void func(int* arr, int row, int col);
```
<br/>

2. 2D 배열을 포인터 배열로 다루기
   - 포인터 배열을 사용하면 열 크기 없이 동적 할당 가능
   - parr[i][j]로 직접 접근 가능, arr처럼 사용 가능
```c
int* parr[3] = { arr[0], arr[1], arr[2] };  // 포인터 배열
```
<br/>

3. 문자열 배열을 함수에 전달하는 방식
   - char* str[] → 자동으로 char**처럼 동작
```c

void print_strings(char** str, int size);
```
   - char arr[][10]처럼 크기 명시한 배열은 직접 넘길 수 있음
```c
void print_fixed_strings(char arr[][10], int size);
```
<br/>

4. const의 의미
   - const int a = 10; → 변수값 변경 불가
   - const int* p; → 포인터가 가리키는 값 변경 불가
   - int* const p; → 포인터 주소 변경 불가
   - const int* const p; → 주소 & 값 변경 불가
<br/>

5. 함수 포인터 사용 (pfunc 관련)
   - operation ops[] = { my_sum, my_max }; → 함수 포인터 배열
   - int (*pfunc)(char*) = NULL; → 단일 함수 포인터
   - 함수 포인터를 사용하려면 pfunc = my_func;로 초기화 필요
